123var = 10 # variable names can't begin with digits
var.abs() # undefined variable
var = 3
if var = 5 # used = instead of ==, missing a colon
    123var == 6 # variable names can't begin with digits, used == instead of =
elif var == 6:
    print_data(b) # undefined variable
    if var = 123var: # used = instead of ==, variable name can't begin with a digit
        pass
while var < 10 # missing a colon
    var += 1
3b == 5 # used == instead of =, variable name can't begin with a digit, undefined variable